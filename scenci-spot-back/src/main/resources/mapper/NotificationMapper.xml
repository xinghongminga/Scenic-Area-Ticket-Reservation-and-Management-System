<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.scencispotback.mapper.NotificationMapper">

  <insert id="insert" parameterType="com.example.scencispotback.domain.Notification" useGeneratedKeys="true" keyProperty="id">
    insert into sys_notification(receiver_id, title, content, ntype, created_by)
    values(#{receiverId}, #{title}, #{content}, #{ntype}, #{createdBy})
  </insert>

  <select id="listForUser" resultType="com.example.scencispotback.domain.Notification">
    select * from sys_notification where receiver_id = #{userId} order by is_read asc, created_at desc
  </select>

  <select id="findById" resultType="com.example.scencispotback.domain.Notification">
    select * from sys_notification where id = #{id}
  </select>

  <update id="markAsRead">
    update sys_notification set is_read=1 where id=#{id}
  </update>

  <update id="markAllAsRead">
    update sys_notification set is_read=1 where receiver_id=#{userId}
  </update>

  <select id="countUnread" resultType="int">
    select count(*) from sys_notification where receiver_id = #{userId} and is_read = 0
  </select>
</mapper>
