<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.scencispotback.mapper.OrderStatusLogMapper">

  <insert id="insert">
    insert into order_status_log(order_id, from_status, to_status, operator_type, operator_id, detail_json)
    values(#{orderId}, #{fromStatus}, #{toStatus}, #{operatorType}, #{operatorId}, #{detailJson})
  </insert>
</mapper>
